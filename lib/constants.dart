import 'package:flutter/material.dart';

// Refined academic palette
const Color primaryColor = Color(0xFF2C3E50); // Navy dark blue for text/buttons
const Color accentColor = Color.fromARGB(
  255,
  57,
  153,
  204,
); // Sky blue for icons & links
const Color bgColor = Color(0xFFF6F9FC); // Very light background
const Color inputFillColor = Color(0xFFEAF3FB); // Input field fill
const Color cardColor = Color(0xCCFFFFFF); // 80% white, slightly transparent
const Color textColor = Color(0xFF1C1F26); // Strong visible text
const Color textHintColor = Color(0xFF45515E); // Darker gray hint text
const Color tabSelectedColor = Color(0xFF4DA8DA);
const Color tabUnselectedColor = Color(0xFF95A5A6);
const Color darkButtonColor = Color(0xFF2C3E50); // For primary buttons
